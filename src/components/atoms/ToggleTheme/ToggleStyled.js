import styled from "styled-components";

const ToggleWrapper = styled.div`
  min-width: max-content;
  max-width: 25%;
  display: flex;
  padding-right: clamp(16px, 1%, 32px);
  justify-content: flex-end;
  align-items: center;
  height: 100%;
`;

export default ToggleWrapper;
